<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.related.RelatedFragment">

    <TextView
        android:id="@+id/EmptyRelationsTextView"
        style="@style/Material3TextWithDrawable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="64dp"
        android:text="@string/empty_relations"
        android:textAlignment="center"
        android:visibility="gone"
        app:drawableTopCompat="@drawable/baseline_link_off_24" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/RelatedRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="4dp" />
</FrameLayout>